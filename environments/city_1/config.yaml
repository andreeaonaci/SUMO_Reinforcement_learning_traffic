name: city_1
net_file: sumo_rl/nets/RESCO/arterial4x4/arterial4x4.net.xml
route_file: sumo_rl/nets/RESCO/arterial4x4/arterial4x4_1.rou.xml
delta_time: 5
num_seconds: 3600
sumo_seed: 42
max_lanes: 16
max_queue: 50.0
max_wait: 300.0
max_speed: 50.0
k_max: 8
max_hops: 3
use_libsumo: true
begin_time: 0.0
# lr intentionally removed so --lr CLI flag controls all cities uniformly.
# If city_1 needs a different rate than the global LR after stable training
# is confirmed, restore this with a value relative to the confirmed LR.
